// Js is a dynamically typed language 
// we dont need to define datatype before assigning value to a variable
// example let id=34;

// give names that make sense dont give small names like abc provide some fruitfull names

/*datatypes are of 2 types

primitive:- String, Number ,boolean ,undefined , null , BigInt ,Symbol ,

reference:- Array , Object, Function
*/




//+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++//

//stack(primitive where we copy data and give to other variable), 

// heap( non primitive where we provide reference of other variable and changes aplies to both of the variables )

// null has a datatype :- object
// date too has a datatype object

// comparison symbols <(smaller than),>(greater than),<=,>=
// equality check == (check equality only),
// ===(check equality with its datatype)

// string is a object 
// let avg = "simar"
// let avg = new String ("simar");
// console.log(avg[2])
// console.log(avg.charAt(3))
// console.log(avg.length);
// console.log(avg.substring(2,5));